name: ict305project # Replace with your preferred environment name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11  # You can adjust the Python version if necessary
  - streamlit
  - requests
  - openpyxl
  - ipython
